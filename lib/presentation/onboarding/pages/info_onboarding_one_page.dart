import 'package:flutter/material.dart';

class InfoOnBoardingOnePage extends StatelessWidget {
  const InfoOnBoardingOnePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.blue,
      ),
    );
  }
}
