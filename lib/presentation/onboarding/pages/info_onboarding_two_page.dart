import 'package:flutter/material.dart';

class InfoOnBoardingThreePage extends StatelessWidget {
  const InfoOnBoardingThreePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.brown,
      ),
    );
  }
}
