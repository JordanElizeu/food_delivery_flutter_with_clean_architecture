import 'package:flutter/material.dart';

class InfoOnBoardingTwoPage extends StatelessWidget {
  const InfoOnBoardingTwoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.red,
      ),
    );
  }
}